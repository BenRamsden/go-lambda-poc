# GraphQL schema example
#
# https://gqlgen.com/getting-started/

scalar Time

type Me {
  id: ID!
  name: String!
  email: String!
}

input Pagination {
  count: Int!
  page: Int!
}

type Query {
  me: Me!
}

type Mutation {
  empty: Boolean
}

